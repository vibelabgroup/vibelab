version: "3.8"

services:
 vibelab:
 image: node:22-alpine
 working_dir: /app
 container_name: vibelab-main
 restart: unless-stopped
 ports:
 - "8082:3000"
 volumes:
 - /docker/vibelab/vibelab:/app
 command: sh -c "npm install && npm run build && npm run preview -- --host 0.0.0.0 --port 3000"
